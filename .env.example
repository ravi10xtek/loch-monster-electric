# CMS
CMS_URL=http://localhost:3001
NEXT_PUBLIC_CMS_URL=http://localhost:3001

# ISR revalidation — must match REVALIDATION_SECRET in lme-cms
REVALIDATION_SECRET=

# Google Maps (used on service area pages and map component)
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=
NEXT_PUBLIC_GOOGLE_PLACE_ID=

# Site URL (used for sitemap, OG tags, canonical URLs)
SITE_URL=https://lochmonsterelectric.com
