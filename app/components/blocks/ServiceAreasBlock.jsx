/**
 * ServiceAreasBlock — renders the static ServiceAreas component.
 * No configurable fields; the block just places the section.
 */
import ServiceAreas from '../ServiceAreas'

export default function ServiceAreasBlock() {
  return <ServiceAreas />
}
